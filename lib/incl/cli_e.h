enum {
CLI_BASE,
CLI_OFFSET,
CLI_INDEX,
CLI_LEAD,
CLI_RULE,
};

enum {
CLI_B,
CLI_W,
CLI_D,
CLI_Q,
};

enum {
CLI_SI,
CLI_DI,
};

enum {
CLI_IN,
CLI_OUT,
CLI_ERR,
};

enum {
CLI_UNDO,
CLI_REDO,
};

enum {
CLI_PIN,
CLI_PASTE,
};
