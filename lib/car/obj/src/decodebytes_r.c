# define CAR_H
# include "./../../../config.h"

signed(__cdecl decodebytes_r(signed char(*arg))) {
auto signed r;
r = db(deref(arg));
r = (r&(contentextent(clo(r))));
return(ordervibyvi(r,decodebytes_rr(++arg)));
}
