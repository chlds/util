# define CAR_H
# include "./../../../config.h"

signed(__cdecl cuev_r(signed char(**sym),signed char(*argp))) {
auto signed r;
r = cueht(argp);
if(r) return(r);
r = cuelf(argp);
if(r) return(r);
r = cuecr(argp);
if(r) return(r);
return(cuev_rr(sym,argp));
}
