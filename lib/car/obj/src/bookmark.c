# define CAR_H
# include "./../../../config.h"

signed(__cdecl bookmark(signed char(**args),signed(argp))) {
auto signed *b;
auto signed r;
b = ledger(args);
r = ribbon(b,argp);
b = burn(b);
return(r);
}
