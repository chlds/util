/* Synopsis

Copy shallowly
*/


# define CAR_H
# include "./../../../config.h"

signed char **(__cdecl catnol(signed char(*args),signed char(**argp))) {
auto signed r;
r = counci(argp);
if(args) r++;
return(anda_xe(args,alle(argp,alloc(++r*(sizeof(*argp))))));
}
