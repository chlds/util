# define CAR_H
# include "./../../../config.h"

signed char *(__cdecl catalyst(signed char(*args),signed char(*argp))) {
return(coop_xe(argp,coop(args,alloc(clb(inc(counc_xe(args,counc(argp))))))));
}
