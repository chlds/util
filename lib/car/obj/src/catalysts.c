# define CAR_H
# include "./../../../config.h"

signed short *(__cdecl catalysts(signed short(*args),signed short(*argp))) {
return(coops_xe(argp,coops(args,alloc(clbs(inc(couns_xe(args,couns(argp))))))));
}
