# define CAR_H
# include "./../../../config.h"

signed char *(__cdecl readlet(signed char(*args),signed(argp))) {
auto signed char perm[] = ("rdonly,binary");
return(xtt(perm,args,argp,readletters));
}
