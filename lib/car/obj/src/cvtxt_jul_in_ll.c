# define CAR_H
# include "./../../../config.h"

signed char *(__cdecl cvtxt_jul_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Iul."); // in Carolingian minuscule
auto signed char a[] = ("IVL");
b = (a);
if(arg) b = (c);
return(catt(b,(void*)0x00));
}
