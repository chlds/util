# define CAR_H
# include "./../../../config.h"

signed char *(__cdecl trim(signed char(*arg))) {
auto signed char *b;
auto signed r;
b = reve(arg);
r = cii(river(b));
b = lethe(b);
return(convey(arg,r+(counc(arg))));
}
