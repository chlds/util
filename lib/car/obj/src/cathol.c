/* Synopsis

Copy shallowly
*/


# define CAR_H
# include "./../../../config.h"

signed char **(__cdecl cathol(signed char(*args),signed char(**argp))) {
auto signed r;
r = counci(argp);
if(args) r++;
return(alle_xe(argp,anda(args,alloc(++r*(sizeof(*argp))))));
}
