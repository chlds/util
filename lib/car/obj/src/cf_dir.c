/* **** Notes

Confirm
*/


# define CAR_H
# define STDIO_H
# include <errno.h>
# include "./../../../config.h"

signed(__cdecl cf_dir(signed char(*argp))) {
auto signed char *dir = ("dir");
if(!argp) return(0x00);
if(!(already_in_b(argp))) return(0x00);
return(cf_ff(argp,dir));
}
