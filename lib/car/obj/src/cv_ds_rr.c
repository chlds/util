/* **** Notes

Convert into an array of double words out of data type size_t.

Remarks:
Call fn. rl later.
Refer at fn. cv_sd.
//*/


# define CAR
# include <stdio.h>
# include "../../../incl/config.h"

signed(__cdecl cv_ds_rr(signed(*argp))) {

auto signed *d;
auto signed r;
auto signed short flag;

AND(r,0x00);
if(!argp) return(0x00);

return(cv_ds_rr_r(r,argp));
}
