# define CAR_H
# include "./../../../config.h"

signed char *(__cdecl cvtxt_sep_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Sept.");
auto signed char a[] = ("SEPT");
b = (a);
if(arg) b = (c);
return(cathy(b));
}
