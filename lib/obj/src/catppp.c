/* **** Notes

Call fn. catpp

*/


# include "./../../../incl/config.h"

int signed(__cdecl catppp(char signed(***argp))) {

/* **** DATA, BSS and STACK */
auto int signed(r);

/* **** CODE/TEXT */
if(!argp) return(0);
if(!(*argp)) return(0);

catpp(*argp);

argp++;
return(1+(catppp(argp)));
}
