# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_iii_in_ll(signed(argp))) {
auto signed char **w;
auto signed char l[] = ("III");
auto signed char l_decor[] = {
0xC9,0xAA,0xC9,0xAA,0xC9,0xAA,\
0x00,
};
auto signed char *(a[]) = {
l_decor,
l,
0x00,
};
w = (a);
if(!argp) w++;
return(cathy(*w));
}
