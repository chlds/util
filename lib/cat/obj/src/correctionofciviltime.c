# define CAT_H
# define TIME_H
# include "./../../../config.h"

time_t(__cdecl correctionofciviltime(time_t(arg))) {
auto time_t e;
if(arg<(0x00)) return(0x00);
e = mktime(chronos(UNIQUE,&arg));
return(e+(cii_te(mktime(chronos(COMMON,&arg)))));
}
