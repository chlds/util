# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_mon_decor_in_ll(signed(argp))) {
auto signed char a[] = {
0xCA,0x9F,0x00,
};
return(cathy(a));
}
