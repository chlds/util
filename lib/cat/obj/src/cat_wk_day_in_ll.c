# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_wk_day_in_ll(signed char(*argp))) {
auto signed char *b;
auto signed r;
auto signed char *mdot = (MDOT);
auto signed char sp[] = (" ");
auto signed char a[] = ("DIES");
b = (0x00);
if(!argp) return(argp);
return(cat_xe(cath(a,sp,mdot,sp,b),argp,b));
}
