# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_oct_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Oct.");
auto signed char a[] = ("OCT");
b = (a);
if(arg) b = (c);
return(cathy(b));
}
