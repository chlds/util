# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed(__cdecl countleapyearsuntil_xe(signed(arg))) {
auto signed r;
AND(r,N);
ADD(r,divfloor(arg,IV));
ADD(r,cii(divfloor(arg,C)));
ADD(r,divfloor(arg,CD));
return(r);
}
