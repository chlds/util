# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_nov_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Nov.");
auto signed char a[] = ("NOV");
b = (a);
if(arg) b = (c);
return(cathy(b));
}
