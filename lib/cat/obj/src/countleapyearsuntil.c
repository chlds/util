# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed(__cdecl countleapyearsuntil(signed(arg))) {
auto signed r;
AND(r,N);
ADD(r,arg/(IV));
ADD(r,cii(arg/(C)));
ADD(r,arg/(CD));
return(r);
}
