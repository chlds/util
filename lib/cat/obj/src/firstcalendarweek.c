# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

time_t(__cdecl firstcalendarweek(signed(argt),signed(args),time_t(argp))) {
return(weekfloor(argt,args,argp+(cii_te(spw(dec(weeknumber(argt,args,argp)))))));
}
