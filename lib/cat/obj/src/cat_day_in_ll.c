# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_day_in_ll(signed(argp/* e.g., case */))) {
auto signed char a[] = ("DIES");
return(cathy(a));
}
