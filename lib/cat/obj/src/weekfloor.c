# define CAT_H
# define TIME_H
# include "./../../../config.h"

time_t(__cdecl weekfloor(signed(argt),signed(args),time_t(argp))) {
return(dayfloor(args,argp+(cii_te(spd(weekphase(argp,args,argt))))));
}
