# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_word_separator(signed(arg))) {
auto signed char *b;
auto signed char sp[] = (" ");
auto signed char *mdot = (MDOT);
b = (0x00);
if(arg) return(cath(sp,mdot,sp,b));
return(cath(sp,b));
}
