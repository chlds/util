# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_jan_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Ian.");
auto signed char a[] = ("IAN");
b = (a);
if(arg) b = (c);
return(cathy(b));
}
