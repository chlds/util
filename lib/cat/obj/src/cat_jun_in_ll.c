# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_jun_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Iun."); // in Carolingian minuscule
auto signed char a[] = ("IVN");
b = (a);
if(arg) b = (c);
return(cathy(b));
}
