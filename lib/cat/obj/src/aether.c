# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed(__cdecl aether(signed(args),void(*argp))) {
auto signed(__cdecl*c)(void(*argp));
auto signed(__cdecl*(e[]))(void(*argp)) = {
aethersecond,
aetherminute,
aetherhour,
aetherdayofthemonth,
aethermonth,
aetheryear,
aetherdayoftheweek,
aetherdayoftheyear,
aetherdaylightsavingtime,
0x00,
};
if(!argp) return(0x00);
c = (*(e+(args%(counci((signed char**)(e))))));
return(c(argp));
}
