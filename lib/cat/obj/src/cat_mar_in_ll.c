# define CAR_H
# define CAT_H
# define TIME_H
# include "./../../../config.h"

signed char *(__cdecl cat_mar_in_ll(signed arg)) {
auto signed char *b;
auto signed char c[] = ("Mart.");
auto signed char a[] = ("MART");
b = (a);
if(arg) b = (c);
return(cathy(b));
}
